import React from 'react';
import ConfirmBasic from './ConfirmBasic'

const ConfirmsSamples = () => {
    return (
        <div>
            <p>Looking in React Dev Tools you will see that Confirm is build over Modal which is nice code reuse</p>
            <h2>Basic confirm</h2>
            <ConfirmBasic/>
        </div>
    );
};

export default ConfirmsSamples;