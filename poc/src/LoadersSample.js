import React from "react";
import {Loader } from "semantic-ui-react";

const LoadersSample = () => {
  return (
    <div>
      <Loader active>Loading ...</Loader>
    </div>
  );
};

export default LoadersSample;
