import React from "react";
import CardsBasic from './CardBasic'

const CardsSample = () => (
    <div>
        <CardsBasic/>
        <h2 style={{color:'red'}}>add also cards group</h2>
        
    </div>
    );

export default CardsSample;
